# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env

# Конфіденційні нотатки з API-токенами Civitai (НЕ комітити)
civitai.red.md
# Локальні нотатки з токенами — не комітити. Факти з них витягнуті в worker/README.md.
Хроніка розгортання*.md
history.md
docker.md
new.md

# Кореневий Dockerfile містить Civitai-токен відкритим текстом (використовуйте worker/Dockerfile)
/Dockerfile

# Тестові згенеровані зображення
RunPod_*.png

# NSFW-плейсхолдери галереї — не заливаємо в публічний репозиторій (apple-test.png лишаємо)
client/public/examples/example-*.png

# Логи проксі (можуть містити промпти)
runpod-proxy/logs/

# Експорти персональних пресетів (приватні, можуть містити промпти)
presets-*.json
